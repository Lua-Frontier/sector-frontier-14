<Control xmlns="https://spacestation14.io"
         xmlns:customControls="clr-namespace:Content.Client.Administration.UI.CustomControls;assembly=Content.Client"
         HorizontalExpand="True"
         VerticalExpand="True">
    <BoxContainer Orientation="Vertical"
            VerticalExpand="True"
            HorizontalExpand="True"
            SeparationOverride="4"
            Margin="4">
        <BoxContainer Orientation="Horizontal" HorizontalExpand="True">
            <TextureRect Name="DisciplineTexture"
                    TextureScale="2 2"
                    VerticalAlignment="Center"
                    Margin="2"/>
            <EntityPrototypeView Name="TechnologyTexture"
                         Scale="2 2"
                         VerticalAlignment="Center"
                         Margin="4"
                         SetSize="48 48"
                         MaxSize="48 48"/>
        </BoxContainer>
        <Label Name="TechnologyNameLabel" HorizontalExpand="True"/>
        <Label Name="TechnologyCostLabel" HorizontalExpand="True"/>
        <Button Name="ResearchButton"
                Text="{Loc 'research-console-menu-server-research-button'}"
                HorizontalExpand="True"
                MinHeight="32"
                ClipText="True"
                Margin="0 4 0 0"/>
        <Label Text="{Loc 'research-console-required-text'}" HorizontalAlignment="Center"/>
        <customControls:HSeparator StyleClasses="LowDivider" Margin="0 2 0 4"/>
        <BoxContainer Name="RequiredTechContainer" Orientation="Vertical" HorizontalExpand="True">
            <RichTextLabel Name="NoPrereqLabel"
                           HorizontalAlignment="Center"
                           Text="{Loc 'research-console-no-tech-requirements'}"
                           Visible="True"/>
        </BoxContainer>
        <Label Text="{Loc 'research-console-unlocks-text'}" HorizontalAlignment="Center"/>
        <customControls:HSeparator StyleClasses="LowDivider" Margin="0 2 0 4"/>
        <ScrollContainer HScrollEnabled="False"
                         HorizontalExpand="True"
                         VerticalExpand="True">
            <BoxContainer Name="UnlocksContainer"
                          Orientation="Vertical"
                          HorizontalExpand="True"/>
        </ScrollContainer>
    </BoxContainer>
</Control>
