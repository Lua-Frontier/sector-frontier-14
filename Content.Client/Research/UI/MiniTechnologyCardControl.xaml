<Control xmlns="https://spacestation14.io">
    <BoxContainer Orientation="Horizontal">
        <PanelContainer Name="Background"
                        Access="Public"
                        StyleClasses="PdaBackground"
                        VerticalExpand="False"
                        HorizontalExpand="False"
                        MaxWidth="10"
                        Margin="0 0 -5 0"/>
        <Button Name="Main"
                Disabled="True"
                HorizontalExpand="True"
                VerticalExpand="False"
                StyleClasses="ButtonSquare"
                Margin="0"
                ToolTip="foobar">
            <BoxContainer Orientation="Horizontal" Margin="0">
                <EntityPrototypeView Name="TechnologyDisplay"
                             HorizontalExpand="False"
                             VerticalExpand="False"
                             Margin="1"
                             Scale="1 1"
                             MaxSize="64 64"/> <!-- Frontier: swap icons to be proto based --> <!-- Lua: Added MaxSize="64 64" -->
                <Control MinWidth="5"/>
                <RichTextLabel Name="NameLabel" StyleClasses="LabelSubText" VerticalAlignment="Center"/>
            </BoxContainer>
        </Button>
    </BoxContainer>
</Control>
