<DefaultWindow xmlns="https://spacestation14.io"
               xmlns:controls="clr-namespace:Robust.Client.UserInterface.Controls;assembly=Robust.Client"
               Title="{Loc 'company-reveal-window-title'}"
               SetSize="440 160"
               MinSize="380 140">
    <BoxContainer Orientation="Vertical" Margin="8" SeparationOverride="8">
        <controls:RichTextLabel Name="RequestText" Access="Public" VerticalExpand="True" />
        <BoxContainer Orientation="Horizontal" HorizontalExpand="True" SeparationOverride="8">
            <Control HorizontalExpand="True" />
            <Button Name="DeclineButton" Text="{Loc 'company-reveal-decline'}" MinWidth="120"/>
            <Button Name="AcceptButton" Text="{Loc 'company-reveal-accept'}" MinWidth="120"/>
        </BoxContainer>
    </BoxContainer>
</DefaultWindow>